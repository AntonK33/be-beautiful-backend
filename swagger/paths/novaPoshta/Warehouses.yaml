
      tags:
        - NP
      summary: Get a list of branches using the city link
      operationId: Warehouses
      security: []
      parameters:
        - in: path
          name: cityRef
          required: true
          schema:
            type: string
          description: Unique identifier (Ref) of the city
      responses:
        200:
          description: Successful response (list of warehouses)
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
        500:
          description: Internal Server Error