tags:
  - Clients
summary: Create a new client
operationId: addClientsController
security: []
requestBody:
  required: true
  content:
    application/json:
      schema:
        $ref: '../../components/schemas/clients/ClientRequest.yaml'
responses:
  '201':
    description: Client created successfully
    content:
      application/json:
        schema:
          $ref: '../../components/schemas/clients/Client.yaml'
  '400':
    description: Invalid input